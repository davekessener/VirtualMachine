#ifndef EDITOR_H
#define EDITOR_H

#include <iostream>
#include <vector>
#include "Screen.h"
#include "Keyboard.h"
#include "Banner.h"

class Editor
{
	public:
		static int run(const std::vector<std::string>&);
	private:
};

#endif

