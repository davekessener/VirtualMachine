#include "AxisAlignedBoundingBox.h"
#include "Tessellator.h"

void AABB::render(DWORD id, DWORD sides) const
{
	Tessellator &t(Tessellator::instance());

//	float u = (id % 
//		float u = (id % 2) * 8 / 16.0, v = (id / 2) * 8 / 16.0;
}

